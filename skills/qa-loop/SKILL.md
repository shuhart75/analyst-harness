# QA Loop

Run kind: `qa`

Inputs: slice requirements, acceptance criteria, task candidates, implementation state, prototype.

Writes: test plan, coverage matrix, executable/manual checks, classified gaps.

Rules: every check traces to a requirement; failures are classified as requirement gap, implementation defect, test defect, data/environment issue, or accepted limitation.
