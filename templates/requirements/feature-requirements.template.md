# Требования по feature — <Русское название фичи>

Статус: **draft**  
Feature: `features/<feature-slug>/feature.md`  
Квартал: `<YYYY-QN>`  
Дата обновления: `<YYYY-MM-DD>`  
Шаблон: `.workflow/templates/requirements/feature-requirements.template.md`

## Оглавление

Используй заголовки до уровня `####`.

## Общий контур feature

- Назначение feature:
- Что уже есть в baseline/current:
- Какая дельта добавляется этой feature:
- Какие slice входят в контрольный документ:
- Какой общий feature prototype используется как визуальная база:

## Порядок slice для контроля

1. `<01> <slice-slug> — <Русское название среза>`
2. `<02> <slice-slug> — <Русское название среза>`
3. `<03> <slice-slug> — <Русское название среза>`

---

## <STORY/Jira/feature-slice-id> — <Русское название slice> (Заголовок 2)

Slice card: `slices/<slice-slug>/slice.md`  
Детализация FE: `slices/<slice-slug>/requirements/frontend.md`  
Детализация BE: `slices/<slice-slug>/requirements/backend.md`  
Общий prototype: `prototype.html`  
Slice prototype: `slices/<slice-slug>/delivery-prototype/prototype.html`  
Planning story: `planning/stories/STORY-<FEATURE>-NNN.md`

**Бизнес-требования**

- Цель:
- Бизнес-правила:
- Ограничения:

**Пользовательские требования к АС КОДА**

- 

**Критерии приемки**

| Acceptance ID | Критерий |
|---|---|
| AC-<FEATURE>-<SLICE>-001 |  |

**USE CASES**

- **осн. сценарий 1**
- **альт. сценарий 1.1**
- **альт. сценарий 1.2**
- **осн. сценарий 2**

### Функциональные требования

Каждое проверяемое правило получает стабильный `REQ-*` ID, который используется в task candidates, implementation plan и test coverage.

#### Реализация для FRONTEND

**Описание UI**

| Экран | Результат |
| --- | --- |
|  |  |

Требования на фронт:

- 

Связанный детальный FE pack: `slices/<slice-slug>/requirements/frontend.md`

#### Реализация BACKEND

Требования на бэк:

- 

Связанный детальный BE pack: `slices/<slice-slug>/requirements/backend.md`

| | |
| --- | --- |
| **Сервис** | `<Название сервиса / bounded context>` |
| ИФТ URL | `<если известен>` |
| База данных | `<ссылка или файл с моделью данных>` |
| Методы | `<ссылка на OpenAPI / список методов>` |

**Изменения в ролевой модели**

- Что меняется:
- Что остаётся без изменений:
- Если изменений нет, зафиксируй это явно.

**Отправка в Аудит**

- Какие события должны аудироваться:
- Какие новые методы или сценарии попадают в аудит:
- Если изменений нет, зафиксируй это явно.

**BUGS (Если есть)**

- 

## Доработки затронутых фич

Все обязательные cross-feature изменения входят в scope и HLE текущей фичи.

| Impact ID | Затронутая фича | Причина | Требуемая доработка | Срез/компонент | FE/BE/QA влияние | Критерий завершения | Planning/task coverage | Статус синхронизации |
|---|---|---|---|---|---|---|---|---|
| IMP-<FEATURE>-001 |  |  |  |  |  | AC-<ID> | STORY-/CAND-/TEST- | pending |

### Доп. пояснения

- Этот документ является главным feature-level документом для контроля требований.
- Содержимое slice cards и detail packs является производным от этого документа.
- Если детализация slice выявила новый scope, сначала обнови этот root document, а потом синхронизируй slice artifacts.
- Подробности по FE/BE не дублируй без необходимости: выноси их в slice detail packs и оставляй здесь краткий управленческий контрольный слой.
- Если у feature один slice, всё равно используй этот же шаблон без упрощения структуры.
- Cross-feature доработки не оставляй только в `domain-impact.md`: включай их в scope текущих требований и покрывай task candidates и проверками.
- Для каждого slice сохраняй порядок разделов и не переставляй их без явного основания в пользовательском сценарии или lifecycle.
- Confluence-специфичные макросы `UI Expand`, раскраска текста и Jira-встраивания в Markdown не воспроизводятся буквально; в markdown-версии их заменяет явная структура разделов, ссылки на detail packs и текстовые пометки о статусе/нереализованных местах.
