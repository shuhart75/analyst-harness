Switch to planning mode.
Read `AGENTS.md`, `.analyst-workspace.json`, `.workspace-state/active-mode.md`, `modes/planning.md`, and relevant project rules in the configured analytical repository.
Then inspect the target `feature`, `planning stories`, existing quarter gantt files, and scope prototype.
